{
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "name": "counter-fragment",
  "private": true,
  "scripts": {
    "build": "mkdir -p build && cp src/* build",
    "format": "prettier --write ."
  },
  "version": "1.0.0"
}